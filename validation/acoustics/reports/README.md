# Acoustic validation reports

Commit compact machine-readable results and Markdown summaries needed to reproduce an acceptance
decision. Do not commit large exploratory plot batches or optimiser output directories.

Every report identifies the BladeAD commit, fixture revision, model configuration, declared
applicability envelope, accuracy thresholds chosen before evaluation, and pass/fail result.
